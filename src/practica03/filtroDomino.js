const dominoBoton = document.getElementById('domino')

/**
 * Función encargada de transformar la imagen en fichas de domino.
 */
const domino = () => {
    dominoBoton.addEventListener('click', () => {
        console.log('Boton de filtro domino funciona xd.');
    })
}