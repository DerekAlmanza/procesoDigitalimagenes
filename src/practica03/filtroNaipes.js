const naipesBoton = document.getElementById('naipes')

/**
 * Función encargada de transformar la imagen en cartas de naipes.
 */
const naipes = () => {
    naipesBoton.addEventListener('click', () => {
        console.log('Boton de filtro naipes funciona xd.');
    })
}